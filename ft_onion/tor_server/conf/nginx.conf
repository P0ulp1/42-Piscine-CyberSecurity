events {
    worker_connections  1024;
}

http {
    include       mime.types;

    server {

		listen 80;
		listen [::]:80;
		
		root /var/www/html;
		
		location / {
			try_files $uri $uri/ =404;
		}

		index index.html index.htm index.nginx-debian.html;
    }
}